import type { HeadConfig } from 'vitepress'

const head: HeadConfig[] = [
  ['link', { rel: 'icon', href: '/assets/favicon.ico' }],
  ['meta', { name: 'author', content: '组件库' }],
  ['meta', { name: 'keywords', content: '组件库' }],
]

export default head